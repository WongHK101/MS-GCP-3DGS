diff --git a/submodules/diff-quadratic-rasterization/cuda_rasterizer/forward.cu b/submodules/diff-quadratic-rasterization/cuda_rasterizer/forward.cu
index 05c1637..3be9048 100644
--- a/submodules/diff-quadratic-rasterization/cuda_rasterizer/forward.cu
+++ b/submodules/diff-quadratic-rasterization/cuda_rasterizer/forward.cu
@@ -681,13 +681,15 @@ renderCUDA(
 				// distortion loss is taken from 2DGS paper, please check https://arxiv.org/pdf/2403.17888.pdf
 				A = 1-T;
 				float error = mapped_max_t * mapped_max_t * A + dist2 - 2 * mapped_max_t * dist1;
-				C[DISTORTION_OFFSET] += error * alpha * T;
 				
 				dist1 += mapped_max_t * alpha * T;
 				dist2 += mapped_max_t * mapped_max_t * alpha * T;
-				// median depth
-				
-				C[DEPTH_OFFSET] += root * alpha * T;
+				const float camera_z = depths[collected_id[j]];
+				const float weight = alpha * T;
+				C[DEPTH_OFFSET] += camera_z * weight;
+				C[DISTORTION_OFFSET] += camera_z * camera_z * weight;
+				if (camera_z > 0.0f)
+					C[MIDDEPTH_OFFSET] += weight / camera_z;
 			}
 			
 			if (return_normal){ // and curvature
@@ -733,7 +735,6 @@ renderCUDA(
 		
 			}
 			if (T > 0.5){
-				C[MIDDEPTH_OFFSET] = root;
 				median_weight = alpha * T;
 				median_contributor = contributor;
 			}
diff --git a/submodules/diff-quadratic-rasterization/cuda_rasterizer/stopthepop_QGS/resorted_render.cuh b/submodules/diff-quadratic-rasterization/cuda_rasterizer/stopthepop_QGS/resorted_render.cuh
index fab0bf3..17eafa7 100644
--- a/submodules/diff-quadratic-rasterization/cuda_rasterizer/stopthepop_QGS/resorted_render.cuh
+++ b/submodules/diff-quadratic-rasterization/cuda_rasterizer/stopthepop_QGS/resorted_render.cuh
@@ -25,7 +25,6 @@ __device__ __inline__ void updateDistortionMap(
 	float w = alpha * T;
     float mapped_max_t = (FAR_PLANE * depth - FAR_PLANE * NEAR_PLANE) / ((FAR_PLANE - NEAR_PLANE) * depth);
     float error = mapped_max_t * mapped_max_t * A + dist2 - 2 * mapped_max_t * dist1;
-	all_map[DISTORTION_OFFSET] += error * w;
 
 	dist1 += mapped_max_t * w;
 	dist2 += mapped_max_t * mapped_max_t * w;
@@ -49,11 +48,8 @@ __device__ __inline__ void updateMap(
 	float w = alpha * T;
     for (int ch = 0; ch < CHANNELS; ch++)
 		all_map[NORMAL_OFFSET + ch] += normal[ch] * w;
-    all_map[DEPTH_OFFSET] += depth * w;
 	all_map[ALPHA_OFFSET] += w;
 	all_map[CURVATURE_OFFSET] += w * curvature;
-    if (T > 0.5)
-        all_map[MIDDEPTH_OFFSET] = depth;
 }
 
 
@@ -159,11 +155,16 @@ renderkBufferCUDA(
         }
         updateDistortionMap(sort_depths[0], sort_alphas[0], T, sort_curvs[0], dist1, dist2, curv1, curv2, C);
         updateMap<CHANNELS>(sort_depths[0], sort_alphas[0], T, sort_normals, sort_curvs[0], C);
+        const float camera_z = depths[sort_ids[0]];
+        const float weight = sort_alphas[0] * T;
+        C[DEPTH_OFFSET] += camera_z * weight;
+        C[DISTORTION_OFFSET] += camera_z * camera_z * weight;
+        if (camera_z > 0.0f)
+            C[MIDDEPTH_OFFSET] += weight / camera_z;
         for (int ch = 0; ch < CHANNELS; ch++)
 			C[ch] += features[sort_ids[0] * CHANNELS + ch] * sort_alphas[0] * T;
 
         if (T > 0.5){
             median_contributor = contributor;
-			C[MIDDEPTH_OFFSET] = sort_depths[0];
 			
 		}
