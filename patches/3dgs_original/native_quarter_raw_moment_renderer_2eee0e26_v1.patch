diff --git a/gaussian_renderer/__init__.py b/gaussian_renderer/__init__.py
index f74e336..eeb7d5d 100644
--- a/gaussian_renderer/__init__.py
+++ b/gaussian_renderer/__init__.py
@@ -15,7 +15,15 @@ from diff_gaussian_rasterization import GaussianRasterizationSettings, GaussianR
 from scene.gaussian_model import GaussianModel
 from utils.sh_utils import eval_sh
 
-def render(viewpoint_camera, pc : GaussianModel, pipe, bg_color : torch.Tensor, scaling_modifier = 1.0, override_color = None):
+def render(
+    viewpoint_camera,
+    pc : GaussianModel,
+    pipe,
+    bg_color : torch.Tensor,
+    scaling_modifier = 1.0,
+    override_color = None,
+    return_raw_metric_depth_accumulators = False,
+):
     """
     Render the scene. 
     
@@ -45,7 +53,8 @@ def render(viewpoint_camera, pc : GaussianModel, pipe, bg_color : torch.Tensor,
         sh_degree=pc.active_sh_degree,
         campos=viewpoint_camera.camera_center,
         prefiltered=False,
-        debug=pipe.debug
+        debug=pipe.debug,
+        return_raw_metric_depth_accumulators=return_raw_metric_depth_accumulators,
     )
 
     rasterizer = GaussianRasterizer(raster_settings=raster_settings)
@@ -82,7 +91,7 @@ def render(viewpoint_camera, pc : GaussianModel, pipe, bg_color : torch.Tensor,
         colors_precomp = override_color
 
     # Rasterize visible Gaussians to image, obtain their radii (on screen). 
-    rendered_image, radii = rasterizer(
+    raster_outputs = rasterizer(
         means3D = means3D,
         means2D = means2D,
         shs = shs,
@@ -91,10 +100,17 @@ def render(viewpoint_camera, pc : GaussianModel, pipe, bg_color : torch.Tensor,
         scales = scales,
         rotations = rotations,
         cov3D_precomp = cov3D_precomp)
+    if return_raw_metric_depth_accumulators:
+        rendered_image, radii, raw_metric_depth_accumulators = raster_outputs
+    else:
+        rendered_image, radii = raster_outputs
 
     # Those Gaussians that were frustum culled or had a radius of 0 were not visible.
     # They will be excluded from value updates used in the splitting criteria.
-    return {"render": rendered_image,
+    output = {"render": rendered_image,
             "viewspace_points": screenspace_points,
             "visibility_filter" : radii > 0,
             "radii": radii}
+    if return_raw_metric_depth_accumulators:
+        output["raw_metric_depth_accumulators"] = raw_metric_depth_accumulators
+    return output
